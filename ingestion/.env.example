# Supabase connection for the ingestion layer.
# Use the service_role key for unrestricted server-side inserts, or the anon
# key if you rely on the permissive hackathon RLS policies.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-service-role-or-anon-key
